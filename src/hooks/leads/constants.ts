// Static filter options matching DB CHECK constraints
// Note: business_type is fetched dynamically via useBusinessTypes hook

export const FORM_OPTIONS = ['Book a Demo', 'Contact Us'] as const

export const EMPLOYEE_COUNT_OPTIONS = ['1-10', '11-50', '51-100', '100+'] as const
